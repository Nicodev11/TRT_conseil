{% extends 'base.html.twig' %}

{% block title %}Announce{% endblock %}

{% block body %}
    <div class="container">
        <div class="announcement_name mb-3">
            <h2 class="text-center text-dark fs-1">{{ announcement.name }}</h2>
        </div>
        <div class="announcement_description list-group list-group-flush">
            <div class="row">
                <div class="col-lg-8 mb-3">
                    <div class="description">
                        <p class="fs-4 text-black">Description</p>
                        <p class="ms-5">{{ announcement.description|nl2br }}</p>
                    </div>
                </div>
                <div class="col-lg-4">
                    <p class="fs-4 text-black"> Plus d'informations</p>
                    <ul class="list-group mb-5">
                        <li class="list-group-item bg-body-tertiary">
                            <p class="m-0"> Date de publication : {{ announcement.createdAt|date('d/m/Y') }} </p>
                        </li>
                        <li class="list-group-item bg-body-tertiary">
                            <p class="m-0"> Type du contrat : {{contractName}} </p>
                        </li>
                        <li class="list-group-item bg-body-tertiary">
                            <p class="m-0">Nombre d'heures : {{announcement.hours}}h</p>
                        </li>
                        <li class="list-group-item bg-body-tertiary">
                            <p class="m-0">Salaire mensuel : {{announcement.salary}}€ </p>
                        </li>
                    </ul>
                    <div class="d-grid">
                        {% if is_granted('ROLE_USER') %}
                            <button class="btn btn-success" type="button"><i class="bi bi-arrow-right-circle-fill"></i> Postuler</button>
                        {% elseif is_granted('ROLE_CONSULTANT') %}
                            
                        {% endif %}
                    </div>

                    
                </div>
            </div>
        </div>
    </div>

    <div class="btn_action d-flex float-end me-5">
        <div class="btn btn-outline-primary my-2 me-2">
            <a class="text-decoration-none text-reset" href="{{ path('announcement_edit', {'id': announcement.id}) }}">Modifier</a>
        </div>
        {{ include('announcement/_delete_form.html.twig') }}
    </div>
    
{% endblock %}
